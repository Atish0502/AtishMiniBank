# Copy to .env and fill values for local development
# Do NOT commit .env to source control

# PostgreSQL connection string (e.g. Neon)
DATABASE_URL=postgresql://<db_username>:<db_password>@<host>/<database>?sslmode=require

# Admin password for admin login (override default)
ADMIN_PASSWORD=atish1997
